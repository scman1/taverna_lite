<%= form_for(@feature_model) do |f| %>
  <% if @feature_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature_model.errors.count, "error") %> prohibited this feature_model from being saved:</h2>

      <ul>
      <% @feature_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
